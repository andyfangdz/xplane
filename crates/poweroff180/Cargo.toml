[package]
name = "poweroff180"
version = "0.8.3"
edition.workspace = true
license.workspace = true
description = "Deterministic SR20 power-off 180 guidance"

[dependencies]
uom.workspace = true
xplane-airports.workspace = true

[dev-dependencies]
flate2 = "1"
