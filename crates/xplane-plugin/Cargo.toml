[package]
name = "xplane-plugin"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared safe utilities for native X-Plane plugins"
readme = "README.md"

[dependencies]
xplane-sdk-sys.workspace = true
