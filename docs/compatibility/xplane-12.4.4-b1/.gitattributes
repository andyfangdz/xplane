# Preserve the bytes covered by the retained evidence hashes on every checkout.
* -text whitespace=blank-at-eol,space-before-tab,cr-at-eol
# Matplotlib emits valid SVG path data with trailing spaces.
*.svg -whitespace
